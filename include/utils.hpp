#ifndef UTILS_HPP
#define UTILS_HPP

#include <vector>
#include <string>

// Return a char vector from a string.
std::vector<char> strToVector(const std::string& s);

// Clear terminal
void clear();

#endif